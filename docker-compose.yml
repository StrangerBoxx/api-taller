version: '3.8'
services:
  api:
    build: .
    container_name: api_dev
    ports:
      - "5001:5001"
    networks:
      - devops-net

networks:
  devops-net:
    external: true
    name: devops-net